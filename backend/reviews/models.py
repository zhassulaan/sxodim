from django.db import models

# Create your models here.
# class Impression(models.Model):
#     """Upcoming impression."""
#     title = models.CharField(max_length=70, help_text="The title of the impression.")
#     info = models.CharField( verbose_name="The discription of the impression.")
#     badges = models.CharField(max_length=20, verbose_name="The badges of the impression.")
#     favourites = models.BooleanField(max_length=20, verbose_name="Saved or not.")
#     date = models.DateField(max_length=20, verbose_name="Date the impression will be held.")
